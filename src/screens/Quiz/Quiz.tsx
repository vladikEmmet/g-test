import React from 'react';
import {QuizController} from "@/components/QuizController/QuizController";

export const Quiz = () => {
    return (
        <div>
           <QuizController />
        </div>
    );
};